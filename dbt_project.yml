name: 'beejan_rides'
version: '1.0.0'

profile: 'beejan_rides'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         
  - "dbt_packages"

models:
  beejan_rides:
    example:
      +materialized: view
